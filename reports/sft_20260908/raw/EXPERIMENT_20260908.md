# SFT 完整实验：2026-09-08

## 已启动

- 北京时间 16:29 启动，后台父进程 PID 2428。
- 服务器：root@connect.bjb1.seetacloud.com，SSH 端口 13681。
- 代码：/root/workspace/test/src/SFT_run_20260908。
- 本次输出：/root/autodl-fs/posttrain/sft-full/20260908_162916_198718。
- 控制台日志：/root/autodl-tmp/sft_experiment_20260908.log。
- 阶段状态：/root/autodl-tmp/sft_experiment_20260908.status。
- 自动跟进：当前任务每 10 分钟检查；状态无明显变化时不通知。

## 实验设置

Qwen2.5-Math-1.5B Base，GSM8K 官方 train 固定划分为 6725 道训练题和 748 道验证题。没有丢弃样本，最长训练样本 635 token，没有触发分段。

3 epochs；micro-batch=1，梯度累积8，有效批量8（每轮最后一组5）；每轮841更新，共2523更新。峰值学习率1e-5，3% warmup 后线性衰减，梯度裁剪1.0，AdamW，FP32参数/状态，BF16 autocast，梯度检查点。每200步和每轮末验证并保存断点；验证loss/entropy覆盖748题，生成准确率固定50题。

训练前测同协议基线，按验证准确率优先、loss次优保存best_model，另存final_model。完成训练后自动对 Base 和 best_model 各运行1319道官方测试题，测试集不用于调参。50道验证生成题的准确率粒度为2个百分点，不能把微小波动当作可靠提升。

## 准备与改动

- 复用服务器 posttrain 环境；torch 2.13.0+cu132、transformers 5.16.1、datasets 5.0.1。
- 安装 tensorboard 2.21.0。
- 原train文件为Git LFS指针，已下载真实7473题数据；禁用Xet后完成下载。
- 9项核心/数据/评分检查通过。
- 使用最长训练样本，以正式训练精度完成一次梯度更新，loss有限、梯度有限、参数实际变化；峰值显存28.88 GiB。此检查不作为正式训练效果。
- 加入每步用时和验证进度日志，核心core.py未修改。
- 为避免数据盘保存大型优化器断点时空间不足，输出放文件存储（启动时约198GB可用）。

## 待结果完成

### 批量执行切换

用户授权后，在第600步验证与断点完整保存后暂停。新增批量padding/attention mask和按实际样本数加权的路径，通过CPU梯度一致性检查及原9项检查。GPU短测同一批样本：micro-batch=1/2/4分别约1.090/0.717/0.599秒每更新，最长8条样本压力检查全部通过，峰值约28.84GiB。选择micro-batch=4、每次更新仍8条样本（两次反传），从600步恢复；不改学习率、数据划分与训练轮数。短测计算耗时降低约45%，不代表含验证和存储的总耗时同比下降。实际恢复步速继续观察。

初步实测：前35步，排除前5步后平均每次更新1.155秒；纯训练约49分钟。计入约15次后续验证、断点写入和两遍完整测试，初估全实验4–7小时，需随后修正。训练前验证loss=0.81417；固定50题答案正确率4%、格式率28%、生成截断率2%。此处不是最终测试结果。

记录实际总耗时、最佳step/epoch、训练与验证loss、Base与best模型测试准确率/格式率/截断率。结合过拟合、未收敛和输出格式问题解释后续参数调整；不预先宣称3轮一定提升。
