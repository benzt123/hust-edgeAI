# 批量性能测试记录

从2026-09-08已记录的服务器测试输出整理；不是重新运行的测量。

| micro-batch | 每次更新样本数 | 每次更新秒数 | 最长样本检查峰值GiB |
| --- | --- | --- | --- |
| 1 | 8 | 1.0902 | 28.8340 |
| 2 | 8 | 0.7170 | 28.8342 |
| 4 | 8 | 0.5991 | 28.8348 |

硬件报告为RTX4090、49140MiB显存。FP32参数和AdamW状态、BF16 autocast、梯度检查点。测试脚本见 `../../src/sft/benchmark_batching.py`，具体软件版本见versions.json。

每种设置执行4组更新，剔除第1组计时，另外使用最长8条训练样本检查显存。测试很短，只用于选择当前执行批量；不代表不同硬件或所有序列长度的普遍性能。峰值为PyTorch allocated口径，不是nvidia-smi进程总占用。最终选择micro-batch=4，有效batch仍为8，从600步断点恢复。
